				 <% if @user.is_parent?  %>
				 
				 You're a parent, let's find your kid.

 
					 <%#= form_for ParentChild.new do |f| %>
					 <%#=f.hidden_field :parent_id, value: user.id %>
					 <%#=f.text_field :child_id %>
					 <%#= f.submit "Claim Kid", class: "btn btn-blue" %>
					 <%# end %>
 
				 <% end %>