<%= render 'shared/background_photo_tab', photo_owner: @user %>

<!-- <div class="profile-info" style="z-index: 999999;">
  <div class="profile-photo">
    <%= image_tag(@user.avatar, height: "100", width: "100") %>
  </div>
  <div class="profile-text light">
    Sports Facility
    <span class="caption"><%= @user.name %></span>
  </div>
</div> -->


  <div class="bg-overlay"></div><!--.bg-overlay-->

  <!--.profile-info-->

  <div class="social-buttons">
    <%= render 'shared/fan' %>
  </div>

  <div class="row">
    <div class="col-sm-5">
       <%= render 'shared/upcoming_event', object: @user %>
    </div>
    <!--.col-->
    <div class="col-sm-6">
      <ol class="breadcrumb">
        <li><a href="/"><i class="ion-home"></i></a></li>
        <li><a href="<%= users_path %>">users</a></li>
        <% if current_user %>
          <% if current_user.id == User.find(params[:id]).id %>
            <li><a href="#" class="active">My Profile</a></li>
          <% else %>
            <li><a href="#" class="active"><%= @user.full_name %></a></li>
          <% end %>
        <% else %>
           <li><a href="#" class="active"><%= @user.full_name %></a></li>
        <% end %>
      </ol>
    </div>
    <!--.col-->
    <% if current_user %>
    <% if current_user == @user %>
      <div class="col-sm-1 text-right">
        <a href="<%= edit_user_path(@user) %>" class="btn btn-floating btn-light-blue">
          <i class="ion-android-settings"></i>
        </a>
      </div>
    <% end %>
    <% end %>
  </div>
  <!--.row-->