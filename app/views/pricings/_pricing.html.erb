<% if object.price.blank? %>
<%= render 'pricings/form', object: object %>
<% else %>
<%= object.name %> 
<b><%= number_to_currency(object.price) %></b>
<%= link_to "Edit Price", pricings_path(object: object.class.to_s, object_id: object.id, event: "edit"), remote: true, class: "btn" %>

<% end %>