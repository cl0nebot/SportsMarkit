<%= form_for pricing_path(object), remote: true do |f| %>
	<%= object.name %> 
	<%= hidden_field_tag :object, "#{object.class.to_s}"%>
	<%= hidden_field_tag :id, object.id %>
	<%= text_field_tag :price, object.price %>

<%= f.submit "Add Price" %>
<% end %>