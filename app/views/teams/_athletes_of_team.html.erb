	<ul class="list-material has-hidden">
		<% members.each do |m| %>
		<% if current_user.can_edit_team?(team) %>
       		<%= render 'teams/admin_view_member', m: m%>
		<% else %>
			<%= render 'teams/member', m: m%>
		<% end %>
    	<% end %>
	</ul>