<% if @announcement.persisted? %>
  $('#announcement-list > tbody').append('<%= j render(@announcement) %>');
  $('#new_announcement')[0].reset()
  $('#sent-message').html("<%= j render 'announcements/successfully_sent', object: @announcement, count: @user_count %>")
<% end %>