  <%= form_for TeamFacility.new, html: {id: 'team-facility-form', data: {remote: true}} do |f|   %>
    <%= f.hidden_field :team_id, value: team.id %>
    <%= f.hidden_field :facility_id, value: facility.id %>
    <%= f.submit "Add Facility"%>
  <% end %>
			  