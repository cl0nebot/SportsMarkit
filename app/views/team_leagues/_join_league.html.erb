<%= form_for TeamLeague.new, html: {method: :post, remote: true } do |f| %>
	<%= f.hidden_field :team_id, value: team.id %>
	<%= f.hidden_field :league_id, value: league.id %>
	<%= league.name %>
		<%= button_tag class: "btn btn-blue btn-sm pull-right" do %>
	 Join <i class="ion-plus-circled pull-right margin-left-10"></i>
	<% end %>
	<hr/>
<% end %>