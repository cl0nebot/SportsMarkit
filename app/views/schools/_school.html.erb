
<%= link_to school.name, school %> |

<%= school.email %> |

<%= school.website %> |

<%= school.phone_number %>

<%= link_to "Delete", school, method: :delete, data: {confirm: "Are you sure?"}, remote: true %> |

<%= link_to "Edit", edit_school_path(school) %>
<br><br>
