<% unless @valid_phone_number %>
    $('#invalid-phone-message').css({ display: "block" })
<% end %>

<% if @blank %>
  $('#blank-role-message-new').css({ display: "block" })
  $('#roster-spot-not-created').css({ display: "block" })
<% else %>
  // $('#roster-spot-created').css({ display: "block" }).fadeOut(4000);
  // $('#roster-form')[0].reset()
  <% if Rails.env.production? %>
    window.location.assign("http://www.sportsmarkit.com/teams/<%= @team.slug %>#tab_roster");
     window.location.reload()
  <% else %>
    window.location.assign("http://localhost:3000/teams/<%= @team.slug %>#tab_roster");
    window.location.reload()
  <% end %>
<% end %>

$('#roster-form')[0].reset();
$('.mobile_phone_number_field').html("");
$( ".add_team_member" ).attr('disabled', true);
team_admin_validation();