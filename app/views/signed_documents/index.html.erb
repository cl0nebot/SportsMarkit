<div class="content">
  <div class="page-header full-content bg-blue mb0">
    <div class="row">
      <div class="col-sm-6">
        <h1>Admin <small>Signed Documents</small></h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb">
          <li><a href="/"><i class="ion-home"></i></a></li>
          <li><a href="<%= users_path %>" class="active">all users</a></li>
        </ol>
      </div>
    </div>
  </div>

  <div class="pt60">
    <div class="row">
      <div class="col-lg-8 col-sm-offset-2">
        <div class="panel box-shadow-ccc">
          <div class="panel-heading">
            <div class="panel-title">
              <h4 class="semi-bold">Signed Documents</h4>
            </div>
          </div>
          <div class="panel-body pb40">
            <div class="">
              <table id="document-list" class="table table-hover table-condensed" id="example">
                <thead>
                  <tr>
                    <th style="width:30%" class="semi-bold h6 text-muted">Name</th>
                    <th style="width:30%" class="semi-bold h6 text-muted">Description</th>
                    <th style="width:30%" class="semi-bold h6 text-muted">File</th>
                    <th style="width:30%" class="semi-bold h6 text-muted">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% if @documents.present? %>
                    <% @documents.each do |document| %>
                      <%= render partial: 'signed_documents/document', locals: { document: document } %>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>