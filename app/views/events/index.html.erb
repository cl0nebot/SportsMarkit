<style>
  .sample-bg-image13:before {
  	background-image: url('<%=image_path("thumbs/football-game")%>');
	}
</style>
<div class="content">
  <div class="page-header full-content bg-blue">
    <div class="row">
      <div class="col-sm-5">
        <h1>All Events <small>(<%= @events.count %>)</small></h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb">
          <li><a href="/"><i class="ion-home"></i></a></li>
          <li><a href="#" class="active">all events</a></li>
        </ol>
      </div>
      <div class="col-sm-1 text-right">
        <a class="btn btn-floating btn-red" data-toggle="modal" data-target="#panel-new-event">
          <i class="ion-android-add"></i>
        </a>
      </div>
    </div>
  </div>
  <div class="row">
    <% @upcoming_events.each do |event| %>
      <div id="rsvp-for-event-<%= event.id %>">
        <%= render event %>
      </div>
    <% end %>
  </div>
</div>
