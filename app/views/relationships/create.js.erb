$('#team-button').empty();
$('#team-button').append('<%= j render 'relationships/pending' %>');
$('.pending-members').empty();

<% @team.pending_members.each do |m| %>
  $('.pending-members').append('<%= j(render 'teams/pending', m: m) %>');
<% end %>